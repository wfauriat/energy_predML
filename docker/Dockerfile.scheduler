FROM python:3.12-slim

WORKDIR /app

COPY docker/requirements-scheduler.txt .
RUN pip install --no-cache-dir -r requirements-scheduler.txt

COPY src/ src/

RUN mkdir -p data/raw data/processed data/predictions data/drift_reports

CMD ["python", "-m", "src.workflows.scheduler"]
