# Serving-only dependencies (api + streamlit).
# Excludes training/tuning/orchestration/monitoring packages:
#   optuna, prefect, lightgbm, scikit-learn, evidently

# Data
requests>=2.31.0
pandas>=2.1.0
duckdb>=0.9.0
pyarrow>=14.0.0

# ML — inference only
xgboost>=2.0.0

# Experiment tracking (model loading)
mlflow>=2.9.0

# Serving
fastapi>=0.104.0
uvicorn>=0.24.0

# UI
matplotlib>=3.7.0
streamlit>=1.29.0

# Utils
python-dotenv>=1.0.0
pydantic>=2.5.0
pydantic-settings>=2.1.0
